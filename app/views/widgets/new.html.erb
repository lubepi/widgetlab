<% content_for :title, "Neues Widget" %>

<div class="container-fluid py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Neues Widget erstellen</h1>
    <%= link_to "Zurück zum Widget Shop", widgets_path, class: "btn btn-outline-secondary" %>
  </div>

  <div class="card shadow-sm">
    <div class="card-body">
      <%= render "form", widget: @widget %>
    </div>
  </div>
</div>
